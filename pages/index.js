import Layout from '../components/layout'

export default () => (
  <Layout>
    <h1>NextAuth.js Example</h1>
    <p>
      This is an example to demonstrate how to use <a href={`https://next-auth.js.org`}>NextAuth.js</a> for authentication.
    </p>
  </Layout>
)